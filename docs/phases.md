🟩 Phase 1 – Auth & Users (Core Identity)

Goal: Solid user identity & JWT auth so everything else can assume a user.

Functional requirements covered:

User register and login – JWT auth

User can reset password (start with email token stub or simple “reset token” endpoint)
